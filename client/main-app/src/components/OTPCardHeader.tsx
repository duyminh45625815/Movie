
import {CardHeader,  CardTitle} from "@/components/ui/card";
export default function OTPCardHeader() {
    return (
        <CardHeader className="text-center">
            <CardTitle className="text-3xl font-bold drop-shadow-glow">Nhập mã OTP</CardTitle>
        </CardHeader>
    );
  
  }